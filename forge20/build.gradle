plugins {
    id 'net.minecraftforge.gradle' version '[6.0,6.2)'
    id("com.github.johnrengelman.shadow") version "8.1.1"
}

java.toolchain.languageVersion = JavaLanguageVersion.of(17)

forgeVersion = "1.20.1"

minecraft {
    mappings channel: 'official', version: '1.20.1'
}

sourceSets.main.resources { srcDir 'src/generated/resources' }

repositories {}

dependencies {
    minecraft "net.minecraftforge:forge:1.20.1-47.1.44"

    implementation 'com.github.EnvyWare.ForgePlaceholderAPI:api:2.0.4'

    implementation group: 'org.spongepowered', name: 'configurate-yaml', version: '4.0.0'

    shadow group: 'com.envyful.api', name: 'commons', version: '5.1.2'
    shadow (group: 'com.envyful.api', name: 'forge20', version: '5.1.2') {
        transitive = false;
    }

    shadow project(':api')
}

shadowJar {
    configurations = [project.configurations.shadow]
    setArchiveClassifier('')
    setArchiveBaseName("${rootProject.name}-Forge")

    relocate('org.spongepowered.configurate', 'com.envyful.tab.forge.shade.configurate')
    relocate('org.yaml.snakeyaml', 'com.envyful.tab.forge.shade.snakeyaml')
    relocate('io.leangen.geantyref', 'com.envyful.tab.forge.shade.geantyref')
    relocate('com.google.gson', 'com.envyful.tab.forge.shade.gson')
    relocate('com.zaxxer', 'com.envyful.tab.forge.shade.hikari')
    relocate('org.slf4j', 'com.envyful.tab.forge.shade.slf4j')
    relocate('com.envyful.api', 'com.envyful.tab.forge.shade.envy.api')
    relocate('org.bstats', 'com.envyful.tab.forge.shade.bstats')
    relocate('com.google.common', 'com.envyful.tab.forge.shade.guava')
    relocate('org.apache.logging', 'com.envyful.tab.forge.shade.log4j')
    relocate('io.netty', 'com.envyful.tab.forge.shade.netty')
    relocate('io.lettuce', 'com.envyful.tab.forge.shade.lettuce')
    relocate('com.cronutils', 'com.envyful.tab.forge.shade.cronutils')
    relocate('com.cronutils', 'com.envyful.tab.forge.shade.cronutils')
    relocate('com.sun.el', 'com.envyful.tab.forge.shade.sun.el')
    relocate('javax', 'com.envyful.tab.forge.shade.javax')
    relocate('javax', 'com.envyful.tab.forge.shade.javax')
    relocate('org.reactivestreams', 'com.envyful.tab.forge.shade.reactivestreams')
    relocate('reactor', 'com.envyful.tab.forge.shade.reactor')

    exclude "**/module-info.class"
    exclude "**/*.properties"
    exclude "**/*.xml"
    exclude "META-INF/versions/9/**"
    exclude "META-INF/native-image/**"
    exclude "META-INF/services/**"
}

jar.finalizedBy('shadowJar')
shadowJar.finalizedBy('reobfJar')
build.finalizedBy('versionedRelease')